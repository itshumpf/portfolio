<section id="csi-project" class="csi-gateway">
  <div class="wrap wrap--wide">
    <header class="csi-gateway__header">
      <p class="eyebrow">Featured case study · Wi-Fi CSI</p>
      <p class="csi-gateway__edition">V1 closed · V2 hypothesis registered</p>
    </header>

    <div class="csi-gateway__hero">
      <div class="csi-gateway__story">
        <p class="csi-gateway__signal">Three ESP32 beacons · Two receivers · Eight nights</p>
        <h2><span>99.7%</span> accurate.<br /><em>Wrong question.</em></h2>
        <p class="csi-gateway__lede">
          I built a system that identified Wi-Fi devices by apparent crystal-clock
          imperfections. Then I designed the controls that proved it was measuring
          the physical link instead.
        </p>
        <a class="csi-gateway__cta" href="/csi">
          <span>Open the full investigation</span>
          <span aria-hidden="true">↗</span>
        </a>
      </div>

      <figure class="evidence-card">
        <figcaption>
          <span>The experiment that changed the claim</span>
          <strong>What moves the measurement?</strong>
        </figcaption>
        <div class="evidence-card__plot" role="img" aria-label="Different boards co-located measured 0.2 standard deviations apart; the same board in two rooms measured 19.6 standard deviations apart; the same-position drift floor was 2.5 to 4.9 standard deviations.">
          <div class="plot-row">
            <div class="plot-row__label"><span>Different boards</span><small>co-located</small></div>
            <div class="plot-row__track"><i style="--value: 1%"></i></div>
            <strong>0.2σ</strong>
          </div>
          <div class="plot-row plot-row--accent">
            <div class="plot-row__label"><span>Same board</span><small>two rooms</small></div>
            <div class="plot-row__track"><i style="--value: 98%"></i></div>
            <strong>19.6σ</strong>
          </div>
          <div class="plot-row plot-row--muted">
            <div class="plot-row__label"><span>Drift floor</span><small>same position</small></div>
            <div class="plot-row__track"><i style="--value: 24%"></i></div>
            <strong>2.5–4.9σ</strong>
          </div>
        </div>
        <p class="evidence-card__note">
          The 19.6σ comparison spans nights and carries the drift floor shown above.
          The 0.2σ co-location result is same-instant.
        </p>
      </figure>
    </div>

    <div class="corpus" aria-label="Eight-night replay scale">
      <div><strong>69.5M</strong><span>clean beacon frames replayed</span></div>
      <div><strong>8</strong><span>paired overnight captures</span></div>
      <div><strong>2</strong><span>receiver architectures</span></div>
      <div><strong>29 / 35</strong><span>distinct ideas / independent rejection tests</span></div>
      <div><strong>0</strong><span>spoof-resistance claims made</span></div>
    </div>

    <div class="verdict">
      <div class="verdict__title">
        <span>What survived</span>
        <h3>A short-lived link state.</h3>
      </div>
      <div class="verdict__measure">
        <span>Within the same night</span>
        <strong>88–96%</strong>
        <p>recognition after recent enrollment</p>
      </div>
      <div class="verdict__measure">
        <span>On a held-out night</span>
        <strong>53–64%</strong>
        <p>best channel-feature recognition</p>
      </div>
      <div class="verdict__meaning">
        <span>The security question</span>
        <p>
          A cloned identity does not clone the path. Whether an attacker can reproduce
          that path is the V2 experiment—not a conclusion borrowed from V1.
        </p>
      </div>
    </div>

    <a class="case-door" href="/csi">
      <span>
        <small>Room or Radio?</small>
        Read the build, the controls that broke it, the 69.5-million-frame replay,
        and the 35 independent tests that rejected 29 distinct hypotheses—then see
        the physical-link authentication question that followed.
      </span>
      <strong aria-hidden="true">Enter case study →</strong>
    </a>
  </div>
</section>

<style>
  .csi-gateway {
    position: relative;
    overflow: hidden;
    padding-block: clamp(76px, 11vw, 132px);
    border-bottom: 1px solid var(--border);
    background:
      linear-gradient(180deg, color-mix(in srgb, var(--accent) 5%, transparent), transparent 35%),
      var(--bg);
  }

  .csi-gateway::before {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    opacity: 0.24;
    background-image:
      linear-gradient(var(--border-soft) 1px, transparent 1px),
      linear-gradient(90deg, var(--border-soft) 1px, transparent 1px);
    background-size: 48px 48px;
    mask-image: linear-gradient(to bottom, black, transparent 66%);
  }

  .csi-gateway .wrap { position: relative; }

  .csi-gateway__header {
    display: flex;
    justify-content: space-between;
    gap: 20px;
    padding-bottom: 22px;
    border-bottom: 1px solid var(--border);
  }

  .csi-gateway__edition,
  .csi-gateway__signal {
    color: var(--text-muted);
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .csi-gateway__hero {
    display: grid;
    grid-template-columns: minmax(0, 1.05fr) minmax(380px, 0.95fr);
    gap: clamp(48px, 8vw, 92px);
    align-items: center;
    padding-block: clamp(52px, 8vw, 88px);
  }

  .csi-gateway h2 {
    margin-top: 18px;
    font-size: clamp(3.2rem, 8vw, 7rem);
    line-height: 0.88;
    letter-spacing: -0.075em;
  }

  .csi-gateway h2 span { color: var(--accent); font-family: var(--font-mono); }
  .csi-gateway h2 em { font-style: normal; color: var(--text-primary); }

  .csi-gateway__lede {
    max-width: 57ch;
    margin-top: 30px;
    color: var(--text-secondary);
    font-size: clamp(1rem, 2vw, 1.13rem);
  }

  .csi-gateway__cta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 390px;
    margin-top: 30px;
    padding: 15px 17px;
    background: var(--text-primary);
    color: var(--bg);
    font-size: 0.83rem;
    font-weight: 750;
    text-decoration: none;
  }

  .evidence-card {
    margin: 0;
    padding: clamp(24px, 4vw, 36px);
    background: color-mix(in srgb, var(--bg) 88%, var(--text-primary));
    border: 1px solid var(--border);
    box-shadow: 18px 18px 0 color-mix(in srgb, var(--accent) 10%, transparent);
  }

  .evidence-card figcaption { display: grid; gap: 4px; }
  .evidence-card figcaption span,
  .verdict span,
  .case-door small {
    color: var(--text-muted);
    font-family: var(--font-mono);
    font-size: 0.67rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .evidence-card figcaption strong { font-size: 1.02rem; }
  .evidence-card__plot { display: grid; gap: 20px; margin-top: 34px; }

  .plot-row {
    display: grid;
    grid-template-columns: 96px 1fr 64px;
    gap: 12px;
    align-items: center;
  }

  .plot-row__label { display: grid; }
  .plot-row__label span { color: var(--text-secondary); font-size: 0.72rem; }
  .plot-row__label small { color: var(--text-muted); font-size: 0.64rem; }
  .plot-row__track { height: 7px; background: var(--border-soft); }
  .plot-row__track i { display: block; width: max(3px, var(--value)); height: 100%; background: var(--text-muted); }
  .plot-row--accent .plot-row__track i { background: var(--accent); }
  .plot-row--muted .plot-row__track i { opacity: 0.45; }
  .plot-row > strong { font-family: var(--font-mono); font-size: 0.8rem; text-align: right; }

  .evidence-card__note {
    margin-top: 28px;
    padding-top: 18px;
    border-top: 1px solid var(--border-soft);
    color: var(--text-muted);
    font-size: 0.68rem;
    line-height: 1.5;
  }

  .corpus {
    display: grid;
    grid-template-columns: 1.15fr repeat(4, 1fr);
    border: 1px solid var(--border);
  }

  .corpus > div { display: grid; padding: clamp(20px, 3vw, 30px); }
  .corpus > div + div { border-left: 1px solid var(--border); }
  .corpus strong { color: var(--accent); font-family: var(--font-mono); font-size: clamp(1.35rem, 3vw, 2rem); }
  .corpus span { color: var(--text-muted); font-size: 0.72rem; }

  .verdict {
    display: grid;
    grid-template-columns: 1.05fr 0.75fr 0.75fr 1.25fr;
    margin-top: clamp(48px, 7vw, 72px);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
  }

  .verdict > div { padding: 25px 24px 28px 0; }
  .verdict > div + div { padding-left: 24px; border-left: 1px solid var(--border); }
  .verdict h3 { margin-top: 8px; font-size: clamp(1.35rem, 3vw, 1.9rem); }
  .verdict__measure strong { display: block; margin-top: 8px; color: var(--accent); font-family: var(--font-mono); font-size: 1.55rem; }
  .verdict__measure p, .verdict__meaning p { color: var(--text-secondary); font-size: 0.74rem; line-height: 1.45; }
  .verdict__meaning p { margin-top: 9px; font-size: 0.8rem; }

  .case-door {
    display: flex;
    justify-content: space-between;
    align-items: end;
    gap: 40px;
    margin-top: 52px;
    padding: clamp(26px, 5vw, 46px);
    border: 1px solid var(--accent);
    color: var(--text-primary);
    text-decoration: none;
    transition: background-color 0.16s ease;
  }

  .case-door:hover { background: color-mix(in srgb, var(--accent) 9%, transparent); }
  .case-door > span { display: grid; gap: 9px; max-width: 62ch; }
  .case-door strong { flex: 0 0 auto; color: var(--accent); font-size: 0.85rem; }

  @media (max-width: 800px) {
    .csi-gateway__hero { grid-template-columns: 1fr; }
    .corpus { grid-template-columns: repeat(2, 1fr); }
    .corpus > div:nth-child(3) { border-left: 0; border-top: 1px solid var(--border); }
    .corpus > div:nth-child(n + 4) { border-top: 1px solid var(--border); }
    .corpus > div:nth-child(5) { border-left: 0; }
    .verdict { grid-template-columns: repeat(2, 1fr); }
    .verdict > div:nth-child(3) { border-left: 0; border-top: 1px solid var(--border); padding-left: 0; }
    .verdict > div:nth-child(4) { border-top: 1px solid var(--border); }
  }

  @media (max-width: 560px) {
    .csi-gateway__header { display: grid; }
    .plot-row { grid-template-columns: 82px 1fr 62px; }
    .case-door { align-items: start; flex-direction: column; gap: 18px; }
  }
</style>
